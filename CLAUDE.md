# Jurepi.kr

무료 온라인 도구 허브 (Next.js 15 App Router · SSG · TS strict · Tailwind v4 · next-intl ko/en · 백엔드/DB 없음).
요구사항 단일 소스: `docs/SPEC.md`, `docs/services/**/SPEC.md`, `docs/DESIGN.md`.

## 디자인 컨텍스트 (impeccable)

전략 단일 소스 `PRODUCT.md`(루트) · 시각 단일 소스 `docs/DESIGN.md`. impeccable 작업은 둘을 먼저 읽는다.
- **Register**: `product` (도구 허브 — 디자인이 도구 사용을 보조, 사용성 최우선; 플레이풀 브랜드 성격은 그 위에 얹힘).
- **Design Principles**: ① 색은 의미(카테고리 액센트 6종=정체성, 브랜드 바이올렛 `#6c5ce7`=유일 액션색) ② 마찰 제로·즉시 사용(SSG 셸 위 클라이언트 SPA) ③ 발견성이 성장(도구별 인덱싱 URL·SEO+GEO[검색엔진+AI 답변 인용]·CWV CLS<0.1) ④ 놀이를 명료하게 하는 모션(compositor 속성·`prefers-reduced-motion` 존중) ⑤ 수익은 콘텐츠를 막지 않음(광고 높이 예약·동의 게이팅).
- **라이브 모드**: `.impeccable/live/config.json` 설정됨 → `/impeccable live`. 세션 저널 `.impeccable/live/sessions/`는 로컬 상태.

## 하네스: Jurepi 풀스택 웹 빌드 (클린 아키텍처 + TDD)

**목표:** 클린 아키텍처(도메인→유스케이스→어댑터→프레임워크 계층 분리)와 TDD(테스트 우선)로 Jurepi 플랫폼·대시보드·도구를 구현한다.

**트리거:** Jurepi 플랫폼/대시보드/도구(사다리타기 등) 구현·기능 추가·리팩터링·버그 수정·재실행 요청 시 `jurepi-build` 스킬을 사용하라. 단순 질문이나 단일 파일 사소 편집은 직접 응답 가능.

**구성:** 빌드 팀(`.claude/agents/`: architect·domain-engineer·ui-engineer·platform-engineer·qa-integration) + 전문가(seo-geo-engineer=도구별 발견성, deploy-engineer=Cloudflare 배포) + 스킬(`.claude/skills/`: jurepi-build 오케스트레이터, clean-architecture, jurepi-tdd, nextjs-ssg-platform, design-system-fidelity, integration-qa, seo-geo-optimization, cloudflare-pages-deploy). 모든 에이전트는 opus. 상세는 오케스트레이터 스킬과 각 디렉토리에서 관리한다.

**변경 이력:**
| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-06-29 | 초기 구성 (5인 팀 + 6 스킬 + 오케스트레이터) | 전체 | 풀스택 웹 하네스 신규 구축, 클린 아키텍처+TDD 요구 반영 |
| 2026-06-29 | 사다리타기(ghost-leg) 구현 — 엔진(공정성 chi-square)·reducer·게임 UI·SSG 라우트, 243 테스트/96% 커버리지/빌드 그린 | src/** (제품) | 첫 도구 빌드. 팀 파이프라인 정상 동작 검증 |
| 2026-06-29 | integration-qa 스킬에 "커버리지·테스트 하드 게이트" 추가 | skills/integration-qa | QA가 도메인 커버리지를 전체로 오인해 false PASS 2회 → 전체 커버리지·UI 테스트 존재를 명시적 차단 체크로 |
| 2026-06-29 | 스캐폴드 기본값 강화 (test=`vitest run`, coverage 스코프 `src/**`) | skills/nextjs-ssg-platform | 워치 모드 스크립트 hang + 커버리지 스코프 오염 결함 재발 방지 |
| 2026-06-29 | 오케스트레이터에 "주장≠증명" 검증 원칙 + 작업 분할 지침 추가 | skills/jurepi-build | 에이전트 거짓 PASS 4회(가짜 chi-square·커버리지 false-PASS·"10/11"실제4실패·행에도 "배포완료") + ui 컨텍스트 소진 2회 |
| 2026-06-29 | Vitest/Playwright 스펙 분리, ui E2E `data-testid` 앵커, 죽은검정·로케일매처·clipboard 교훈 | nextjs-ssg-platform · ui-engineer · jurepi-tdd | E2E 그린화 세션에서 드러난 경계·환경 결함 재발 방지 |
| 2026-06-29 | 메인 대시보드 + 공유 셸 구현 — Hero(마스코트 동반)·검색·카테고리 필터·툴카드 그리드(라이브1+준비중6)·빈상태·404, Header/Footer/테마토글/로케일 스위처, 도메인(검색/정렬/테마) TDD. 357 유닛 / 20 E2E / typecheck / 빌드 10페이지 SSG 그린 | src/** (제품) | 플랫폼 메인화면 빌드 |
| 2026-06-29 | "녹색 빌드가 숨긴" 시각/SSR 결함 4건을 리더 직접 검증으로 적발: ① Tailwind v4가 v3 `tailwind.config.ts` 미로드(@config 브리지 누락)→디자인 토큰 유틸 전부 미생성 ② 그리드 CSR-only(useSearchParams+Suspense)→SSG/SEO 회귀 ③ 라이브 카드 링크 로케일 프리픽스 누락→404 ④ spacing 토큰명(md/sm) 충돌로 `max-w-md`=16px | src/app/globals.css · ToolExplorer · ToolCard · Hero/Modal/EmptyState | 빌드·테스트·axe 모두 그린이어도 렌더 화면/SSR HTML을 직접 봐야 함(시각 검증 게이트) |
| 2026-06-30 | 위 4건의 **예방** 패턴을 생성 시점 스킬로 라우팅: @config·CSR그리드·next-intl Link → nextjs-ssg-platform, 토큰명 충돌·@config → design-system-fidelity, test-utils 동기화·useRef 초기값·vitest 전역 import·jsdom URL 격리 → jurepi-tdd | skills/{nextjs-ssg-platform,design-system-fidelity,jurepi-tdd} | 탐지(integration-qa)만으론 재발 방지 부족 — 빌드/생성 단계에서 막도록 인코딩 |
| 2026-06-30 | 헤더/레이아웃 개선 5건 — 전역 검색 콤보박스(HeaderSearch: 모든 페이지 동작·키보드 탐색·ARIA combobox·검색어 하이라이트, matchTool/filterTools/sortTools·Link 재사용), 로케일 활성 표시(useLocale+aria-current), 테마 3상태 아이콘(라이트=해/다크=달/시스템=모니터)+동적 aria-label, 카테고리 좌측정렬(mx-auto max-w-container). 396 유닛 / 5 E2E / typecheck / 빌드 10p SSG 그린 + 시각·SSR 검증 통과 | src/** (제품) | 실사용 헤더 결함 수정. 도구 상세 페이지에서 돋보기 무반응(#tool-search가 홈에만 존재) 등 |
| 2026-06-30 | "주장≠증명" 재발: ui-engineer가 "build/tsc pass·no visual regression" 주장했으나 ① 테스트 파일 tsc 2-error(vitest는 통과) ② 오픈 검색 입력 26px 붕괴(유닛은 role/presence만 검사, width 미검증) → 리더가 직접 `tsc`·E2E boundingBox·스크린샷 Read로 적발·수정 | (검증 프로세스) | 신규 오버레이/입력은 width·boundingBox 시각 게이트 필수, 에이전트 "빌드 통과" 주장은 리더 tsc 재실행 전엔 불신 — integration-qa 강화 후보 |
| 2026-06-30 | 위 두 교훈을 스킬로 인코딩(하네스 진화): ① 탐지 → integration-qa에 "tsc 직접 재실행(vitest 그린≠tsc 그린)" + "신규 오버레이 boundingBox width/정렬 게이트" 하드체크 추가 ② 예방 → design-system-fidelity에 "떠 있는 레이어는 명시적 너비+엣지 앵커, 트리거 래퍼 폭 의존 금지" 패턴+체크리스트 추가 | skills/{integration-qa,design-system-fidelity} | 탐지만으론 재발 방지 부족 — 생성·검증 양쪽에 인코딩 |
| 2026-06-30 | 로고/마스코트 링크 + 검색 좌측 펼침 후속 빌드 — 로고는 홈 유지, 마스코트는 `NEXT_PUBLIC_BLOG_URL`(빈값=기존 유지) 외부 링크, 검색 입력은 돋보기 왼쪽에서 시작. 400 유닛 / 5 E2E / tsc / 빌드 그린 + SSR href 직접 확인 | src/** (제품) | 실사용 링크 정정(로고≠블로그) + 검색 위치 미세조정 |
| 2026-06-30 | 후속 빌드 교훈 3건 스킬 인코딩(하네스 진화): ① 선택적 외부링크는 `NEXT_PUBLIC_*`(렌더 본문에서 읽기·stubEnv 테스트·빌드 인라인) + 관습 뒤집는 링크는 확인 → nextjs-ssg-platform ② 검증 전 환경 위생(stale `.next`/점유된 :3000 → 가짜 500, 서버로그 먼저) → integration-qa ③ 간결한 시각/위치 피드백엔 최소 literal 변경+스크린샷 루프(재설계·추상 질문 금지) → jurepi-build | skills/{nextjs-ssg-platform,integration-qa,jurepi-build} | 이번 세션 시간낭비·왕복 줄이기 위해 인코딩 |
| 2026-06-30 | 사다리 도구 개선 10건 — SPA 원칙 명시, 도움말 기본 접힘(네이티브 `<details>`), 이름 자동추천 모드(과일 이모지+이름), 결과 자동추천(1명 당첨 / 순위=키캡 이모지, 10↑ 2자리), 결과 복사→**이미지 다운로드**(자체완결 SVG→PNG: 보드+결과표), 소리(Web Audio 팝) 실구현(토글이 무동작이던 것), 당첨 컨페티(의존성0·reduced-motion 존중), 버그수정[① 마지막 가로선 바닥 붙음→상·하 마진 대칭(vertical `+1`칸, path 바닥 연장) ② 클릭 trace 사라짐→`revealed`별 정적 path 영속]. 도메인 TDD(name-suggestions·result-suggestions·result-image·sound) + 리듀서 일괄액션 + useLadder 디스패처. 유닛 481 / coverage 91%(도메인 100%) / tsc 0 / 빌드 10p SSG 그린 / E2E 44 (다운로드·기하·trace·도움말·자동추천·컨페티·a11y 10) + 리더 라이브 검증(기하 대칭·trace 영속·PNG 내용·도움말 접힘·자동추천·컨페티·320 무overflow·en) | src/** (제품) · 5인 팀 병렬(domain·ui×4·platform·qa) | 실사용 도구 개선·버그 수정 |
| 2026-06-30 | 하네스 진화 2건: ① **요구 #1 '모든 도구=SSG 셸 위 클라이언트 SPA, 사용성 최우선' 원칙을 nextjs-ssg-platform에 등록**(Server/Client 경계 절) ② 리더 시각검증이 '녹색 테스트가 숨긴' 무한루프 적발 → jurepi-tdd에 인코딩: 훅 반환객체(`useLadder()`)를 effect 의존성에 넣으면 실앱은 매-렌더 새 객체로 무한 재실행(이름 재추첨 깜빡임)인데 고정-prop 단위테스트는 GREEN. 예방=안정참조만 의존 + 훅-소유 부모 Harness로 테스트 | skills/{nextjs-ssg-platform,jurepi-tdd} | 탐지(리더 시각 게이트)만으론 부족 — 생성 단계에서 막도록 인코딩 + 제품 원칙 등록 |
| 2026-06-30 | **배포 전문 에이전트 신설** — deploy-engineer(Cloudflare Pages 정적 배포·트러블슈팅) + cloudflare-pages-deploy 스킬(SKILL + troubleshooting 런북) 추가. 정적 익스포트(`output:'export'`) 전략 채택. 발견한 함정 인코딩: 정적 익스포트는 `src/middleware.ts`(루트→`/ko` 리다이렉트)와 `next.config` `headers()`(보안 헤더 5종)를 조용히 버림 → `_redirects`/`_headers`로 이전 + 배포본 `curl -I` 검증 게이트. platform-engineer/nextjs-ssg-platform에서 '배포' 책임 분리(drift 제거), jurepi-build에 배포 절 추가 | agents/deploy-engineer.md · skills/cloudflare-pages-deploy · agents/platform-engineer · skills/{jurepi-build,nextjs-ssg-platform} | 사용자 요청(CF Pages 배포 담당 에이전트). 배포는 platform-engineer가 선언만 하고 구현 0이던 drift였음 |
| 2026-06-30 | 사다리 trace 직각화 + 참가자/결과 레일 정렬 — ① trace를 대각선→직각(맨해튼) 경로로 레일·가로선 위에 정확히 겹침 ② 칩·보드·카드가 같은 컬럼 트랙 공유(`colCenterPct=(2i+1)/(2N)%`)로 참가자=레일 시작 위·결과=끝 아래 정렬(±1px), 반응형(데스크톱 전체/모바일 스크롤). 정렬이 드러낸 결함 수정: 결과 카드가 인덱스 커플링→`selectInversePermutation`(끝컬럼 도달 플레이어 기준 공개·accent), 숨김 카드 `rotateY(90°)`=투명→가시 '?'. 유닛 662 / tsc 0 / 빌드 12p / E2E 35 + 리더 라이브(±1px·공개짝맞춤·320 스크롤) | src/components/tools/ladder/** · ladderLayout.ts · ladder-reducer | 실사용 시각 결함(선이 사다리와 안 이어짐) 수정 |
| 2026-06-30 | 하네스 진화 2건: ① **E2E 검증 서버는 Playwright `webServer`에 맡긴다** — 리더가 띄운 수동 `pnpm start`가 검증 중 죽어 전체 전멸(단독은 그린)인 가짜 실패, 코드로 오인. `curl`로 서버 생존 확인 + 포트 비워 Playwright가 서버 생애주기 관리(35 그린) → integration-qa ② **위치를 데이터로 정렬하면 인덱스 vs 실매핑 커플링이 드러난다** — 실매핑(역순열)으로 단위 테스트 못박기 + 숨김 상태는 '보이는 플레이스홀더', 비항등 시드로 결정화 → jurepi-tdd | skills/{integration-qa,jurepi-tdd} | 탐지(리더 게이트)로 잡은 결함을 생성·검증 단계에 인코딩 |
| 2026-06-30 | **Q&A a Day(1일1질문) 도구 추가** — 도메인(로컬시간 날짜엔진·365질문은행·불변 저널ops·통계·zod 스키마·직렬화, 123 테스트·도메인 100%) + useDailyJournal(localStorage·격리·즉시저장) + UI(Today/Calendar/Journal/Settings/Intro/HowTo/Faq, SPA 탭) + registry `mindset` 카테고리(grape)·CATEGORY_ORDER·92 i18n키 + 라우트 slug 분기·`generateMetadata`(qna+ladder) + SoftwareApplication·FAQPage JSON-LD. 662 유닛 / tsc 0 / 빌드 12p SSG / **E2E 6 그린** + 리더 라이브검증(렌더·reload 지속성·Cmd+S·ko/en 질문·날짜 로케일·320 무overflow). **'녹색이 숨긴' 결함 6건 리더 적발·수정**: ① `t('_locale')`→`Intl.DateTimeFormat` RangeError로 ErrorBoundary가 도구 전체 삼킴 ② 최상위 `mounted` 게이트가 SEO 본문+JSON-LD를 프리렌더에서 제외 ③ `composer.delete` 키 누락 ④ autosave **스테일 클로저**가 빈 텍스트 저장→엔트리 삭제(지속성 붕괴) ⑤ 이중 디바운스로 "저장됨"이 실제 쓰기 700ms 전 표시(거짓 안전) ⑥ date 변경 시 컴포저 미동기화 + Cmd+S 키 대소문자 | src/{lib,components/tools}/qna-a-day/** · tools/{types,registry}·tool-search·messages·[slug]/page.tsx · 5인팀 병렬 | 도구 추가 |
| 2026-06-30 | 하네스 진화(위 6건 인코딩): ① **클라이언트 도구 섬은 SEO 본문(Intro/HowTo/FAQ)+JSON-LD를 `mounted` 게이트 밖에서 SSR하고 date/localStorage 의존부만 게이트** + **i18n 키를 Intl(`DateTimeFormat`/`toLocaleDateString`)에 넘기지 말 것(로케일은 `useLocale()`→BCP-47)** → nextjs-ssg-platform ② **지속성/autosave는 모의 onSave가 아니라 실제 store+reload(E2E/jsdom localStorage)로 검증; "저장됨"은 실제 쓰기 후에만; 디바운스 콜백은 스테일 상태 클로저 금지(값을 인자로 전달); 모든 `t()` 키↔카탈로그 대조** → jurepi-tdd ③ **인터랙티브 E2E '타임아웃' 시 근인 추측 전에 브라우저 콘솔 에러(ErrorBoundary/RangeError/MISSING_MESSAGE) 먼저 수집; JSON-LD/SEO는 프리렌더 HTML로 확인; `reuseExistingServer`가 stale 빌드 재사용·잠긴 MCP 브라우저 프로파일 정리** → integration-qa | skills/{nextjs-ssg-platform,jurepi-tdd,integration-qa} | 탐지(리더 게이트)만으론 부족 — 생성·검증 양쪽에 인코딩 |
| 2026-06-30 | 사다리타기 개선 4건 — ① 셋업/요약 좌측 색 동그라미 제거(보드 칩/카드 색 유지) ② 다운로드 PNG에 보드 레일 위=참가자 이름·아래=결과 라벨 직접 표기 + '이름→결과' 목록 유지 ③ '결과 가리기'→'결과 섞기'(기본 ON): 별도 `prizeOrder`로 결과 칸 위치 무작위(permutation 기하·chi-square 공정성과 분리), 리빌 '?'→공개 동작 유지 ④ 기본 인원 4→7. vitest 679 / tsc 0 / build SSG / 사다리 E2E 42 그린 + 리더 라이브검증(7명·동그라미 없음·섞기 위치·PNG 내용·ko/en·320). 동시 진행 중인 무관 작업(법적페이지/푸터/registry/seo)과 분리해 ladder 범위만 커밋(공유 i18n은 ladder 키만 부분 스테이징) | src/components/tools/ladder/** · lib/{ladder-reducer,result-image} · i18n(ladder 키) · tests/e2e/ladder-* | 실사용 도구 개선 |
| 2026-06-30 | 하네스 진화 2건('녹색이 숨긴' 결함 인코딩): ① **기본값이 라이브러리 기본 인자+호출부 인자 두 곳에 살면, 무인자 훅 단위테스트는 '기본 인자'만 검증(거짓 GREEN)** — `useLadder(initialCount=7)`로 바꿔도 `LadderGame`이 `useLadder(4)`로 override해 화면은 4, 리더 라이브 스크린샷으로 적발. 예방=실제 마운트 지점으로 기본값 검증 + 호출부 grep → jurepi-tdd ② **동작/계약 변경 시 *기존* E2E가 조용히 깨짐 — 새 스펙 추가는 회귀 커버가 아님** — testid 변경·count 4→7·의미 변경 후 새 스펙만 추가하고 기존 4스펙(옛 testid·count 4·사라진 '가시성 모드') 미갱신으로 전멸, 리더가 전체 suite 재실행으로 적발. 예방=옛 식별자 grep + 기능 전체 기존 suite 재실행 → integration-qa | skills/{jurepi-tdd,integration-qa} | 탐지(리더 게이트)만으론 부족 — 생성·검증 단계에 인코딩 |
| 2026-06-30 | 사다리 긴장도+가시성 — ① '긴장도' 컨트롤(낮음·보통·높음, 기본 높음): canceling decoy 가로줄 쌍으로 시각 밀도↑, 공정성·결과 불변(uniformPermutation 유지·resolveAll===perm·chi-square 그린) ② 가시성 풀처리: 트레이스 중 배경 딤 + 활성 경로 강조(굵게+halo) + 애니 속도 경로길이 비례 + 공개 타이밍 동기(traceDurationMs) ③ 레벨 높이 자동 축소(levelHeightFor)로 고밀도 보드 높이 억제(PNG도). vitest 704 / tsc 0 / 정적 export 빌드 / 사다리 E2E 45 그린 + 리더 라이브(고밀도·딤+강조·기본 높음·en·320). 동시 세션이 그새 법적페이지/푸터·Cloudflare output:export 커밋 → ladder 변경만 분리 커밋 | src/lib/{ladder,ladder-reducer,result-image} · src/components/tools/ladder/{ladderLayout,LadderBoard,LadderGame,LadderSetup,useLadder} · i18n·tests/e2e/ladder-* | 실사용 도구 개선(긴장도 요청) |
| 2026-06-30 | 하네스 진화: ① **미존재 Tailwind 토큰은 조용히 `transparent` 렌더 → 선택/활성 상태가 비가시인데 aria-pressed·유닛·E2E·빌드 모두 그린** — 긴장도 세그먼트가 `bg-surface-secondary`·컬러 접미사 빠진 `bg-accent` + `text-white`로 활성 '높음'이 투명배경+흰글자=안 보임(리더 스크린샷 적발). 예방=실재 토큰만(활성 `bg-brand text-on-brand`/비활성 `bg-surface-muted border-hairline`), 선택 상태 시각 게이트 + className 토큰 grep 대조 → design-system-fidelity·integration-qa ② **`output:'export'`(타 세션 커밋)로 `next start` 비호환 → Playwright webServer 레포 전체 깨짐** → webServer를 `serve out`(배포와 동일 정적 서빙)으로 전환·E2E 45 복구 → playwright.config | skills/{design-system-fidelity,integration-qa} · playwright.config.ts | 탐지(리더 게이트) 결함을 생성·검증·인프라에 인코딩 |
| 2026-06-30 | **Cloudflare Workers 정적 에셋 배포 완료** (라이브 `apps.jurepi.kr`) — `output:'export'` 전환 + `public/_headers`(보안5)·`public/_redirects`(/→/ko)·`.nvmrc`(22)·미들웨어 제거 + metadata(manifest/sitemap/robots) `force-static` + `.env.production` 커밋(`NEXT_PUBLIC_SITE_URL=apps.jurepi.kr`·`NEXT_PUBLIC_BLOG_URL=blog.naver.com/dhan0213` → sitemap/canonical/마스코트 링크 정정) + assets-only `wrangler.jsonc`(`not_found_handling:404-page`). `wrangler dev`(workerd) + 라이브 curl 게이트 통과(/→302, 보안헤더5, 라우트200/미지404, canonical/sitemap=apps). 배포파일만 분리 커밋(동시 세션 사다리 WIP 제외) | next.config·src/app/{manifest,sitemap,robots}·src/middleware(삭제)·public/_{headers,redirects}·wrangler.jsonc·.nvmrc·.env.production | 사용자 CF 배포 요청. CF Workers Builds Git연동이 OpenNext SSR로 자동전환돼 `WORKER_SELF_REFERENCE`로 실패하던 것 정정 |
| 2026-06-30 | 하네스 진화(배포 함정 3종 인코딩 + 런북 현실화): ① **`npx wrangler deploy`는 wrangler 설정 없으면 OpenNext SSR로 자동전환→`WORKER_SELF_REFERENCE`(self-ref 이름=package.json `name`≠CF워커명) 실패** → assets-only `wrangler.jsonc` 커밋으로 정적 경로 강제 ② **`output:'export'`는 metadata route handler에 `export const dynamic='force-static'` 필요** ③ **`NEXT_PUBLIC_*`는 `.env.local`(gitignored)로 프로덕션 미반영 → `.env.production` 커밋(또는 CF 빌드 env)**. + 런북이 'Pages' 전제였던 drift를 실제 채택 경로 'Workers 정적 에셋'(`wrangler.jsonc`/`wrangler dev`)으로 갱신 | skills/cloudflare-pages-deploy/{SKILL,references/troubleshooting} · agents/deploy-engineer | 탐지(리더 라이브 게이트)만으론 부족 — 생성·검증 단계에 인코딩 + 런북 현실화 |
| 2026-06-30 | **파비콘 교체(fighter)·theme-color 통일·GA 동의 배너(opt-in)·도구 JSON-LD url 정정** — ① jurepi-fighter-favicon-pack으로 public 아이콘 8종 교체(파일명 유지=참조 불변) ② theme-color `#f7c74a`(viewport meta + manifest theme_color) ③ GA 동의 배너: 순수 정책 `lib/consent` + `ConsentProvider`/`useConsent` + `ConsentBanner` + 푸터 '개인정보 설정 변경' 재오픈, 미동의 시 GA 미로드(opt-in)·수락 시 로드, localStorage 지속, GoogleAnalytics를 동의 게이팅 ④ 도구 JSON-LD url 하드코딩(`jurepi.kr/tools/*`)→`seo.absoluteToolUrl(locale)`로 `apps.jurepi.kr/<locale>/tools/*`(canonical 일치). vitest 714 / 정적 export 빌드 / wrangler dev+브라우저(미동의=gtag0 · 동의=gtag200+pageview204+배너닫힘+localStorage granted) | src/{lib/consent,components/consent,hooks/useReducedMotion}·analytics·[locale]/layout·Footer·layout·manifest·seo·i18n·tools/{ladder,qna}·public/* · ui-engineer 위임+리더 검증 | 사용자 4건 요청 |
| 2026-06-30 | 하네스 진화(동의 배너 회귀 교훈): ① **새 전역 Context Provider 도입 시 `test-utils`의 `AllTheProviders`에 추가 + 전체 `pnpm test` 필수** — `ConsentReopenButton`(`useConsent`)을 Footer에 끼우자 ConsentProvider 밖 throw로 `Footer.test` 7건 실패, 에이전트는 자기 consent 테스트(10/10)만 부분 실행해 놓침(리더 전체 재실행 적발) → jurepi-tdd ② **에이전트 "N passed"가 부분 실행이면 회귀 증명 아님 → 리더 전체 재실행** + **클라 JSON-LD url ↔ canonical 교차 체크(env 소스, 하드코딩 금지)** → integration-qa | skills/{jurepi-tdd,integration-qa} | 탐지(리더 전체 스위트)만으론 부족 — 생성·검증 단계에 인코딩 |
| 2026-07-01 | **배포 정의 정정: 배포 = `git push`(프로덕션 분기 `main`)** — CF Workers Builds(Git 연동)가 push를 감지해 CF 파이프라인에서 `pnpm build`+`wrangler deploy`를 자동 실행. 로컬 `wrangler deploy`/`wrangler login`은 정상 경로가 아니라 CF 연동이 막혔을 때의 예외 폴백. 기능 브랜치/worktree는 `main`에 병합해야 배포 포함. 배포본 검증은 push 후 CF 빌드 완료 대기→실제 도메인 `curl -I`. 하네스 반영: deploy-engineer(배포 모델 블록)·cloudflare-pages-deploy(🚀 배포 트리거 절 + 배포 방법)·jurepi-build(배포 절) | .claude/agents/deploy-engineer · .claude/skills/{cloudflare-pages-deploy,jurepi-build} · CLAUDE.md | 사용자 정정(배포는 wrangler deploy가 아니라 push) |
| 2026-07-01 | **문서 용어 통일: PRD → SPEC (스펙주도 프로그래밍 도입)** — ① 플랫폼·서비스 요구사항 문서 명칭을 PRD→SPEC으로 통일: 파일 `PRD.md`/`PRD_KR.md` → `SPEC.md`/`SPEC_KR.md`(`git mv`로 이력 보존). 컨벤션 유지: **영문 `SPEC.md`=AI 소비 정본 + 국문 `SPEC_KR.md`=번역** ② 하네스·문서 전반의 "PRD" 용어를 "SPEC"으로 진화(에이전트 5종·스킬 6종·CLAUDE.md·PRODUCT.md·README.md·E2E 주석). `_workspace/` 과거 로그는 사실 보존 위해 미변경 ③ 첫 스펙주도 도구 **new-word(신조어·트렌드 용어사전)** 스펙 신규 작성 — 용어를 마크다운 쌍(`<용어>.md`+`<용어>_en.md`)으로 관리→빌드 타임 생성기가 카탈로그로 굽는 모델, 한/영 대역·검색·MZ/기술 주제·용어 템플릿 포함 | docs/** · .claude/{agents,skills}/** · CLAUDE.md · PRODUCT.md · README.md · tests/e2e · docs/services/text/new-word/SPEC{,_KR}.md | 사용자 요청(스펙주도 프로그래밍 도입 + 용어 통일) |
| 2026-07-01 | **SEO+GEO(도구별 발견성) 하네스 인코딩 — 신규 전문가 에이전트+스킬** — 도구별 검색엔진(SEO) *및* 생성엔진(GEO: ChatGPT·Perplexity·Gemini·Claude·AI Overviews 답변 인용) 노출을 일급 관심사로 등록(원칙 ③에 GEO 추가). ① **seo-geo-engineer**(발견성 경계 전문가, deploy-engineer 패턴) + **seo-geo-optimization** 스킬(+`references/geo-and-structured-data.md`: AI 크롤러 UA 표·llms.txt·JSON-LD 레시피) 신설 ② 오케스트레이터에 발견성 게이트 단계+전문가 호출 절+트리거 키워드(SEO/GEO/구조화 데이터/llms.txt/발견성) ③ integration-qa에 "SEO/GEO 발견성 하드 게이트"(프리렌더 HTML의 고유 메타·유효 JSON-LD[url==canonical]·SSR 롱폼·robots AI크롤러·llms.txt·sitemap) + 경계면 2행 ④ platform-engineer/nextjs-ssg-platform 경계 분리(인프라 메커니즘=platform, 도구별 전략·GEO=seo-geo). **비타협 제1원칙: 메타·JSON-LD·SEO 롱폼은 `mounted` 게이트 밖 SSR**(AI 크롤러는 JS 미실행 → 게이트 안이면 정적 HTML에서 소실, Q&A a Day 실제 회귀 재사용) | agents/{seo-geo-engineer,platform-engineer} · skills/{seo-geo-optimization,jurepi-build,integration-qa,nextjs-ssg-platform} · CLAUDE.md | 사용자 요청(도구별 SEO/GEO가 무엇보다 중요 — 방문자·AI 노출) |
| 2026-07-01 | **New Word(신조어 용어사전) 도구 + 공통 마크다운 렌더러 추가** — `text/new-word` 워크트리, 7인 팀 병렬. ① **공통 `<Markdown>`(`src/components/markdown/`, markdown-to-jsx·raw HTML 차단·DESIGN 토큰·SSR·코드스플릿) + `markdownToPlainText`** 순수 헬퍼 — 전 도구 재사용 자산 신설 ② 도메인(schema/merge/slug/catalog/search/favorites) TDD 98% + 빌드타임 생성기(pair/slug/related 검증, 위반 시 빌드 실패, `content/new-word/*.md` 쌍→terms.generated.json) + 시드 12용어(MZ6+Tech6, ko/en+마크다운 본문) ③ SPA UI(useGlossary+8컴포넌트)·SEO섹션·DefinedTermSet/FAQPage/SoftwareApplication JSON-LD(프리렌더)·llms.txt ④ 레지스트리 new-word(text/mint) live·라우트·generateMetadata. 유닛 1050 / E2E 8 실행 그린 / 도메인98%·new-word97%·markdown100%·전체79.5% / 빌드 SSG. **커밋만(text/new-word 유지, 미배포)** | content/** · scripts/generate-glossary.mjs · src/{components/markdown,components/tools/new-word,lib/new-word,lib/markdown}/** · tools/{registry,seo,i18n}·[slug]/page | 사용자 요청(new-word 구현 + 마크다운 렌더링 공통화) |
| 2026-07-01 | 하네스 진화(위 빌드의 '녹색이 숨긴' 결함 인코딩) — ① **built ≠ wired**: 초점 공유능력은 소비처 배선 + 시드 시연을 라이브 확인(마크다운이 만들어졌으나 TermDetail 미배선·시드 본문 무마크다운) + 병렬 UI/platform은 i18n 키 계약(최상위 `tools.<slug>.title`/`description` 포함) 선행/재조정 + 새 공유모듈 배럴 → **jurepi-build** ② **i18n 최상위 키**(`searchable-tools`가 소비하는 title/description 누락 시 대시보드·푸터에 리터럴 노출) + **E2E는 작성≠실행**(webServer `build&&serve` 타임아웃 → `serve out`+`reuseExistingServer` 선기동해 실제 실행) + 스펙 작성 결함(`hasDefinedTerm`·상호작용 게이트 UI) → **integration-qa** ③ **디바운스 검색 입력은 draft 바인딩**(committed 바인딩=타이핑 유실) + **작은 사용자상태는 즉시 persist**(디바운스=유실) → **jurepi-tdd** ④ **팬텀 토큰 grep 하드게이트**(`surface-hover` 3회째 재발→`surface-sunken`) → **design-system-fidelity** | skills/{jurepi-build,integration-qa,jurepi-tdd,design-system-fidelity} · CLAUDE.md | 탐지(리더 게이트)만으론 부족 — 생성·검증 단계에 인코딩 |
| 2026-07-01 | **URL Encoder(URL 인코딩/디코딩) 도구 — UTF-8 + EUC-KR charset** — `dev/url-encoder` 형제 워크트리, 클린아키+TDD 팀 병렬. ① SPEC에 EUC-KR charset 반영 커밋 + 국문 `SPEC_KR.md` 신설(영문 정본↔국문 번역 컨벤션) ② 도메인(schema·charset·encode·decode·query-parser·recents) TDD — **EUC-KR 디코딩=네이티브 `TextDecoder('euc-kr')`(무의존), 인코딩=CP949 정방향맵을 `TextDecoder` 역매핑으로 런타임 생성(무의존)+동적 import 코드스플릿**(초기 번들 제외, 청크 433B) ③ 훅+인터랙티브 SPA(모드/문자셋/방향/배치/쿼리테이블/최근/단축키)+SSR SEO 섹션(동기 useTranslations) ④ dev 카테고리 활성화·registry·SSG 라우트·generateMetadata ⑤ SoftwareApplication+FAQPage JSON-LD(url==canonical)·llms.txt. 유닛 1289 / 도메인 97.6% / **FULL E2E 78(url-encoder 9 시나리오1~8 + 선존 10 복구)** / tsc 0 / 빌드 24p SSG. 리더 라이브검증: EUC-KR `%C7%D1%B1%DB`→한글·콘솔0·320 무overflow·프리렌더 JSON-LD. **커밋만(dev/url-encoder 유지, 미배포)** | docs/services/dev/url-encoder/SPEC{,_KR} · src/{lib,components/tools}/url-encoder/** · tools/registry·[slug]/page·i18n·public/llms.txt | 사용자 요청(가끔 EUC-KR 필요 + url-encoder 구현) |
| 2026-07-01 | 하네스 진화(url-encoder '녹색이 숨긴' 결함 인코딩) — ① **시맨틱 색 팬텀 `semantic-*`**(실토큰=`danger/warning/success`(+`-ink`), 14곳 재발; `color-tokens.test`가 잡음) → **design-system-fidelity** ② **SSR SEO 섹션은 동기 isomorphic `useTranslations`**(비동기 `getTranslations` 서버컴포넌트는 vitest에서 렌더 불가→SEO 테스트 10 실패; new-word 패턴과도 불일치) → **nextjs-ssg-platform** ③ **런타임 유니온 값→i18n 키 보간 드리프트**(`t(\`charset.${'utf-8'}\`)`≠키 `utf8`→토글에 원시키 노출·MISSING_MESSAGE, 유닛은 구조만 봐 GREEN) + **SSR 섹션 테스트는 NextIntlClientProvider에 *전체 중첩* messages**(플랫키=네임스페이스 미해석, 구조만 단언 시 거짓GREEN) + **비동기 디스패처(process)·누적(addRecent) stale-read**(stateRef 최신읽기 안정콜백 + 함수형 setState) → **jurepi-tdd** ④ **리더는 도구 스펙 1개가 아니라 `npx playwright test` 전체 재실행 + 실패의 선존/회귀 귀속**(qa가 자기 9개만 돌려 선존 10건[푸터 도구링크 다중매칭·qna CalendarPanel testId 누락] 놓침; 공유표면 E2E는 `main` 스코프·카운트는 레지스트리 유도) + **형제 워크트리 서브에이전트 브리프에 절대경로 고정**(cwd 상속으로 엉뚱한 워크트리 방지; `EnterWorktree`는 `.claude/worktrees/`만 관리) → **jurepi-build** | skills/{design-system-fidelity,nextjs-ssg-platform,jurepi-tdd,jurepi-build} · CLAUDE.md | 탐지(리더 시각/전체스위트 게이트)만으론 부족 — 생성·검증 단계에 인코딩 |
| 2026-07-02 | **별별 랭킹(Various Rankings) 도구 — 표 형태·출처/기준일 강조 배너** — `news`(8번째) 카테고리 신설(types·CATEGORY_ORDER·FOOTER·i18n), new-word 패턴 미러. 도메인 lib/rankings(schema/slug/merge/search/catalog/favorites) TDD 97% + 빌드타임 생성기 + content 마크다운 쌍. 시드 2건 **웹 수집**(Agent Arena LLM 에이전트 10 · TIOBE 프로그래밍 언어 12). SPEC 확장 `sourceUrl`(선택). ProvenanceBanner(rose 콜아웃·클릭 sourceUrl·고대비, 표 위)+시맨틱 `<table>`(메달 top-3·caption·scope). SoftwareApplication+FAQPage+ItemList×2/ListItem×22 JSON-LD 프리렌더(url==canonical)·llms.txt. 유닛 1442 / 도메인 97% / tsc 0 / E2E 93 / 정적 export 빌드 그린. **커밋만(미배포)** | docs/services/news/rankings/SPEC{,_KR} · src/{lib,components/tools}/rankings/** · scripts/generate-rankings.mjs · content/rankings/** · tools/{types,registry}·tool-search·seo·layout/Footer·[slug]/page·i18n·llms.txt · 5인팀 병렬 | 사용자 요청(별별 랭킹 구현 + 시드 2건 웹 수집) |
| 2026-07-02 | 하네스 진화(리더 게이트가 '녹색이 숨긴' 결함 4건 적발) — ① **조기 return 뒤 조건부 훅 = Rules of Hooks 위반**(`ranking` null→선택 시 훅 개수 2→3 → ErrorBoundary 크래시. 고정-prop 단위테스트는 null↔present 토글을 안 해 GREEN, E2E도 통과 오인) → 모든 훅은 early return **위**에서 무조건 호출 + 단위/컴포넌트 테스트는 null↔선택 토글로 훅안정성 검증 → **jurepi-tdd** ② **E2E가 크리티컬 요소를 `if(el.isVisible)`로 감싸면 없을 때 블록 통째 스킵=거짓 PASS**(표 미렌더 크래시를 못 잡음) + 프로버넌스/텍스트 단언을 SEO howTo/FAQ 산문과 분리(`mainText.toContain` 금지, 상세 영역·`a[href*=src]`로 스코프) + **리더는 상세를 실제로 열어 브라우저 콘솔 에러(ErrorBoundary catch) 확인** → **integration-qa** ③ **`FOOTER_CATEGORIES`는 `CATEGORY_ORDER`와 별개 하드코딩 배열** → 새 카테고리는 둘 다 추가(홈 그리드는 됐는데 푸터만 누락) → **nextjs-ssg-platform** ④ **표시되는 단일-canonical 필드(sourceNote)는 로케일 중립값으로**(라벨은 i18n 로케일화, 값은 고유명사+ISO 날짜) — canonical 규칙상 EN=KO 강제 시 영어 페이지에 한글 노출 → **jurepi-build/design-system-fidelity** | skills/{jurepi-tdd,integration-qa,nextjs-ssg-platform,jurepi-build} · CLAUDE.md | 탐지(리더 시각/콘솔 게이트)만으론 부족 — 생성·검증 단계에 인코딩 |
| 2026-07-02 | **즐겨찾기(Curated Bookmarks) 도구 추가 — 토픽별 큐레이션 링크 디렉토리** — rankings 패턴 미러(클린아키+TDD). 마크다운 쌍(`<topic>.md/_en.md`)→빌드타임 생성기(gray-matter·zod·pair/URL/섹션·링크 검증, 위반 시 빌드 실패)→bookmarks.generated.json. 도메인 lib/bookmarks(schema/slug/merge/search/catalog/favorites) 118테스트 100%. SPA(토픽 탭·검색·즐겨찾기/최근·localStorage)+상세는 섹션별 링크 행(외부링크 rel=noopener). SEO/GEO: Intro/HowTo/FAQ/JSON-LD(SoftwareApplication·FAQPage·ItemList×토픽) 프리렌더 게이트 밖(eGovFrame의 YouTube/GitHub 링크가 JSON-LD로 프리렌더 노출=GEO). dev·sky·status live·registry/route/generateMetadata·ko/en i18n. 콘텐츠 8토픽: 전자정부 표준프레임워크(공식포털·GitHub·YouTube 13링크)·프롬프트 엔지니어링·컨텍스트 엔지니어링·AI 에이전트 도구(오픈클로·헤르메스·OMC·OMO·OMA·MoAI)·하네스 엔지니어링·프런트엔드·디자인·무료 개발 도구. 전체 1562 통과 / tsc 0 / 빌드 SSG(ko·en) 그린 + 리더 라이브 검증. **커밋만(미배포)** | content/bookmarks/** · scripts/generate-bookmarks.mjs · src/{lib,components/tools}/bookmarks/** · tools/registry·[slug]/page·i18n · 5인팀 병렬 | 사용자 요청(bookmarks 구현 + 전자정부 표준프레임워크·프롬프트/컨텍스트 엔지니어링·AI 에이전트 도구 토픽 추가) |
| 2026-07-02 | 하네스 진화(리더 라이브 게이트가 '녹색이 숨긴' 결함 2건 적발) — ① **상태보관 카탈로그 모듈(`initCatalog`+모듈 `_catalog`)을 훅이 초기화 안 하면 `byId()`가 항상 null** — bookmarks 훅이 `initCatalog(CATALOG)` 미호출 → 카드 선택 링(문자열 비교)은 정상인데 **상세 패널이 전혀 렌더 안 됨**(증상 은닉). 도메인 catalog.test는 initCatalog를 직접 불러 GREEN, tsc·전체 1562 유닛도 GREEN. 예방=런타임 조회는 **무상태**(rankings처럼 `byId(catalog, slug)`/in-state `catalog.find`) 선호, 상태보관이면 마운트 시 init 강제 + **리더 시각 게이트: 카드 클릭→상세 실제 렌더 스크린샷 1장** → **jurepi-tdd·integration-qa** ② **병렬 ui/platform i18n 키 드리프트 재발** — ui가 `search.label`·`tabs.filterLabel`·`list.itemCount`·`list.toggleFavorite`·`empty.noFavorites`를 소비했는데 카탈로그엔 없음 → 런타임 MISSING_MESSAGE(tsc·전체 유닛 GREEN, 컴포넌트가 실카탈로그로 렌더 안 돼 은닉). 예방=컴포넌트 테스트는 **실제 메시지 카탈로그**로 NextIntlClientProvider 렌더(모의 금지) + 리더 병합 후 `t()` 키를 ko/en과 diff → **jurepi-tdd·integration-qa** | skills/{jurepi-tdd,integration-qa} · CLAUDE.md | 탐지(리더 라이브 게이트)만으론 부족 — 생성·검증 단계에 인코딩 |
| 2026-07-02 | **스피드퀴즈 제시어(Speed Quiz) 도구 추가 — 진행자용 파티 게임** — `fun/speed-quiz` 워크트리, 클린아키+TDD 병렬팀. 마크다운 쌍(`<deck>.md/_en.md`)→빌드타임 생성기(gray-matter·zod·쌍/필드/단어수≥10/유니크/슬러그 검증, 위반 시 빌드 실패)→speed-quiz.generated.json. 도메인 lib/speed-quiz(schema/slug/merge/catalog/shuffle[seeded PRNG]/game-reducer/sound/favorites) 190테스트 96%. SPA(덱브라우저·검색·즐겨찾기·설정)+진행자 게임보드(대형 타이머/제시어·정답/패스·키보드 Space/→/←/Esc/?)+요약. 사용자 요청으로 **10카테고리(동물·음식·스포츠[올림픽]·영화·K-pop·나라·직업·브랜드·속담·역사인물)×A팀/B팀 2덱=20덱·200단어**(A≠B) 대결 구성(SPEC 8카테고리→10 확장). SEO/GEO: Intro/HowTo/FAQ+SoftwareApplication·FAQPage JSON-LD 프리렌더(url==canonical). fun·sun·Zap·live·registry/route/generateMetadata·ko/en i18n·llms.txt·E2E 11(시나리오1~5+키보드·사운드·axe). 전체 유닛 1831→(main 병합 후)1875 / E2E 96(공유표면 회귀 0) / tsc 0 / 빌드 SSG 그린 + 리더 라이브검증(ko·en·320px·콘솔0). **배포 완료(apps.jurepi.kr, `push origin fun/speed-quiz:main`→CF 자동, 로컬 main WIP 무손상)** | content/speed-quiz/** · scripts/generate-speed-quiz.mjs · src/{lib,components/tools}/speed-quiz/** · tools/registry·[slug]/page·i18n·llms.txt · tests/e2e/speed-quiz · 병렬팀(domain·content·platform·ui×2·qa) | 사용자 요청(speed-quiz 구현 + 10템플릿 A/B 대결) |
| 2026-07-02 | 하네스 진화(리더 시각/콘솔 게이트가 '녹색이 숨긴' 결함 5건 적발, 2건 재발) — ① **[재발] i18n 최상위 `tools.<slug>.title`/`description` 누락 → 푸터/홈카드/검색(`searchable-tools` 소비)에 리터럴 키 노출**(meta.title만 있고 최상위 title 없음; tsc·유닛 GREEN). i18n 키 계약에 최상위 title/description **필수** → **nextjs-ssg-platform·jurepi-build** ② **[재발] `max-w-{sm,md,lg}`는 커스텀 spacing 스케일과 충돌해 16px 렌더**(GameSetup 패널이 세로 한 글자씩 붕괴). 떠 있는 레이어는 스케일 토큰 금지·명시적 `max-w-[32rem]` → **design-system-fidelity** ③ **런타임 보간 i18n(`{current}/{total}`)을 프레젠테이션 컴포넌트 경계로 넘길 땐 `t.raw()`**(값 없이 `t()` 호출 시 ICU FORMATTING_ERROR 콘솔 폭주·표시 깨짐; 유닛이 라이브 `t`로 렌더 안 해 은닉) → **jurepi-tdd** ④ **리듀서가 status 필드를 반환하면 소비 훅은 phase를 status에서 파생/동기화**(markCorrect가 마지막 단어에서 summary 전환하는데 훅이 phase 미갱신 → 보드가 "11/10"로 멈춤; 고정-액션 단위테스트 GREEN). 예방=**종료까지 플레이하는 상태머신 단말전이 테스트** → **jurepi-tdd** ⑤ **프레젠테이션 리프는 window/전역 리스너 금지**(GameBoard가 오케스트레이터와 중복 keydown 등록=잠재 이중발화). 키보드/전역은 오케스트레이터·훅 소유 → **jurepi-build·design-system-fidelity** | skills/{nextjs-ssg-platform,design-system-fidelity,jurepi-tdd,jurepi-build} · CLAUDE.md | 탐지(리더 게이트)만으론 부족 — 생성·검증 단계에 인코딩 |
| 2026-07-02 | **하네스 원칙 인코딩: 콘텐츠 컬렉션 도구는 엔티티 단위 정적 스포크 페이지를 기본 생성(허브+스포크)** — 발견성의 색인 단위는 "도구"가 아니라 "사람이 검색/AI가 인용하는 엔티티". 인터랙티브 유틸(사다리·url-encoder·speed-quiz)=단일 페이지(엔티티 없음, 분리 금지), 콘텐츠 컬렉션(new-word=용어·rankings=랭킹·bookmarks=토픽)=허브(SPA 탐색)+엔티티별 정적 스포크(`/tools/<tool>/<entity>`, generateStaticParams, 고유 메타, **게이트 밖 SSR 본문**, 엔티티 JSON-LD+BreadcrumbList, 허브 내부링크) 병행. **thin-content 가드**(엔티티 고유 산문 1문단↑, 얇으면 허브에만). 우선순위=검색수요×두께: 신조어 사전>랭킹>즐겨찾기. 서빙 부담 0(정적 익스포트, 빌드 산출물만 증가). seo-geo-optimization에 "허브+스포크" 절+체크리스트+트리거 추가 | skills/seo-geo-optimization · CLAUDE.md | 사용자 질문(즐겨찾기 등 컬렉션 도구를 토픽별로 분리해야 하나 — 색인 단위 결정을 재사용 원칙으로) |
| 2026-07-02 | **신조어 사전(new-word) 허브+스포크 리팩터링 — 용어별 정적 스포크 페이지** — 위 원칙의 첫 적용. 허브(`/tools/new-word`, SPA 탐색) 유지 + 용어별 스포크 `/tools/new-word/<slug>`(16용어×ko/en=**32 SSG 페이지**) 신설. seo.ts 헬퍼 4종(`absoluteEntityUrl`·`buildToolEntityMetadata`·`definedTermJsonLd`·`breadcrumbListJsonLd`) + 스포크 라우트(generateStaticParams 32·generateMetadata) + `NewWordSpoke`(서버 컴포넌트, **게이트 밖 SSR**: 정의·예문·유래·마크다운 본문·관련 스포크 링크·breadcrumb) + 허브 카드 크롤 앵커화(SPA 패널 progressive enhancement) + sitemap 34항목 + `spoke.*` i18n ko/en. 리더 검증: tsc 0 / 유닛 1908 / 빌드 32스포크+허브 / 전체 E2E 98(스포크 2 시나리오 추가) / 프리렌더 HTML(H1·정의·DefinedTerm+BreadcrumbList url==canonical·hreflang·허브 16앵커) / 라이브 a11y 트리 | src/lib/seo.ts · src/app/[locale]/tools/new-word/[term]/page.tsx · src/components/tools/new-word/{NewWordSpoke,TermCard} · sitemap · i18n · tests/e2e/new-word-integration · platform∥ui 병렬 | 사용자 요청(우선순위대로 신조어 사전부터 엔티티별 분리) |
| 2026-07-02 | 하네스 진화('녹색이 숨긴 시각 결함' 재발 — hidden 래핑): ① **크롤 링크로 카드를 만들 때 가시 콘텐츠를 `hidden`(display:none)으로 감싸면 화면 카드가 비는데 tsc·유닛·빌드·프리렌더 grep 모두 그린**(ui가 카드 본문 전체를 `<a className="hidden">`에 넣고 `toHaveClass('hidden')` 유닛으로 고정) → 카드 루트=**가시 앵커**, 인터랙티브(별표)는 앵커 밖 형제(button-in-anchor 무효), onClick preventDefault→SPA → **design-system-fidelity**("크롤 가능 카드" 절) ② **`getByTestId`/`getByText`는 jsdom에서 display:none도 찾아 가시성 회귀를 못 잡음 → 가시성은 E2E `toBeVisible`/a11y 트리로만 게이트; 잘못된 설계를 `toHaveClass('hidden')`로 못박지 말 것** → **jurepi-tdd**. 전체 E2E `toBeVisible`("element is not visible" 5건)로만 적발, 리더가 TermCard 재구성+테스트 교정 | skills/{design-system-fidelity,jurepi-tdd} · CLAUDE.md | 탐지(전체 E2E 게이트)만으론 부족 — 생성·검증 단계에 인코딩 |
| 2026-07-03 | **랭킹(rankings) 허브+스포크 — 랭킹별 정적 스포크(2랭킹×ko/en=4)** + **즐겨찾기(bookmarks) 허브+스포크 — 토픽별 정적 스포크(8토픽×ko/en=16)** 배포. 허브(SPA) 유지 + 엔티티별 스포크(고유 메타/canonical/hreflang·게이트 밖 SSR[표·출처 / 설명·섹션 외부링크]·ItemList+BreadcrumbList JSON-LD url==canonical·허브 크롤 앵커). 기존 seo 헬퍼 재사용(absoluteEntityUrl·buildToolEntityMetadata·itemListJsonLd·breadcrumbListJsonLd). 두 허브 모두 `mounted` 게이트 제거로 카드 SSR. bookmarks E2E 스펙 신규(기존 0). 최종 유닛 2135 / 전체 E2E 109 + rankings 9·bookmarks 3 / 라이브 검증(apps.jurepi.kr). 신조어 사전(前 32스포크)까지 **콘텐츠 컬렉션 3종=52 엔티티 스포크** 완료 | src/app/[locale]/tools/{rankings,bookmarks}/[*]/page.tsx · src/components/tools/{rankings,bookmarks}/** · sitemap·i18n·tests/e2e · platform∥ui 병렬 | 사용자 요청(우선순위대로 rankings→bookmarks 분리) |
| 2026-07-03 | 하네스 진화(스포크 리팩터링 함정 4종 인코딩 — 2~3개 도구서 반복 적발) — ① **허브 `mounted` 게이트가 스포크 크롤 앵커를 프리렌더에서 지움** → 카드 그리드 게이트 밖 SSR(빈초기+useEffect 로드 훅은 하이드레이션 안전), `out/…/<hub>.html`에 앵커 N개 grep ② **스포크 SSR 컴포넌트는 동기 useTranslations**(비동기 getTranslations=vitest 렌더 불가→테스트 전멸·삭제, rankings 실측) ③ **상태보관 catalog는 스포크 라우트서 무상태 `CATALOG.find`**(byId는 initCatalog 미호출 시 null→404, bookmarks) ④ **카드 role/태그 변경 시 그 셀렉터로 카드 찾는 타 컴포넌트가 조용히 깨짐**(`<div role=button>`→`<a>`로 TermList 키보드 로빙 라이브 고장→전체 유닛 재실행+옛 셀렉터 grep) → seo-geo-optimization "스포크 구현 함정" 절 + design-system-fidelity·jurepi-tdd·integration-qa 연동 | skills/seo-geo-optimization · CLAUDE.md | 탐지(리더 프리렌더/전체 스위트 게이트)만으론 부족 — 생성·검증 단계에 인코딩 |
| 2026-07-03 | **QR 코드 생성기(qr-code) 도구 추가 — converter(sky) 클라이언트 SPA** — 텍스트/URL/Wi-Fi/vCard/이메일/SMS 6모드(모드별 인코딩·이스케이핑)·라이브 canvas 미리보기·ECC(L/M/Q/H)·크기/quiet-zone·fg/bg 색상+팔레트+ΔE 명도가드·중앙 로고·PNG/SVG 다운로드+클립보드·키보드 단축키·SSR Intro/HowTo/FAQ. 도메인 lib/qr-code(format·encoder[qrcode **BitMatrix**: `.modules.size`+`.get(r,c)`, 2D배열 아님]·contrast[ΔE]·svg-export·schema, 148유닛·~98%) + useQRCode(디바운스·localStorage·안정콜백) + registry(order 21)·라우트·generateMetadata·i18n ko/en·SoftwareApplication+FAQPage JSON-LD(url==canonical)·llms.txt. 217유닛 / tsc 0 / 빌드 SSG(ko·en) / **E2E 8+a11y ko·en** / 공유표면(홈·헤더) 회귀 0. 배포(apps.jurepi.kr, `push origin converter/qr-code:main`→CF; 동시 병합된 age-calculator와 리베이스 충돌 registry+route 5곳 양쪽 보존 해소) | src/{lib,components/tools}/qr-code/** · tools/registry·[slug]/page·i18n·public/llms.txt · 형제 워크트리 병렬팀(architect·domain·ui×2·platform·seo·qa) | 사용자 요청(qr-code SPEC 구현·전용 워크트리) |
| 2026-07-03 | 하네스 진화(리더 시각/E2E 게이트가 '녹색이 숨긴' 결함 적발) — ① **주 액션 버튼 활성상태를 비반응 ref(`canvasRef.current`)에 게이팅하면 영구 비활성**(ref는 리렌더 트리거 안 함→채워져도 버튼 안 켜짐; 채워진 ref를 모의한 단위테스트는 GREEN, 다운로드=핵심기능이 화면에서 불능). 활성상태는 **반응 상태**(result/svg)로 게이팅, 클릭 핸들러만 ref 가드; 라이브 E2E는 버튼 **ENABLE + 다운로드 이벤트 발화** 단언 → **jurepi-tdd·integration-qa** ② **E2E 환경위생 심화**: `reuseExistingServer:true` + 타 워크트리 세션이 점유한 :3000(내 도구 없는 stale `out/` 서빙)→균일 404/textarea 타임아웃 **가짜 전멸실패**(qr-code만 단독은 8/8 GREEN); 죽은 백그라운드 serve→100% 균일 ~1s 실패. **serve가 기대 포트에 실제 바인딩됐는지**(점유 시 랜덤 포트로 조용히 폴백) + `curl /<locale>/tools/<slug>`==200 확인 **후에만** 실패를 코드결함으로 신뢰 → **integration-qa** ③ **UI SSR FAQ + seo StructuredData 양쪽이 FAQPage JSON-LD 방출=중복 구조화데이터** → JSON-LD **단일 소유(StructuredData)**, 프리렌더에 FAQPage 정확히 1개 grep → **jurepi-build·seo-geo-optimization** ④ 재발 강화: i18n 키 드리프트(UI가 `modes.label`·`ecc.help`·`logo.help` 지어냄→런타임 MISSING_MESSAGE, tsc·유닛·빌드 GREEN; 리더가 전체 `t()` 키 grep vs 카탈로그 + 라이브 콘솔)·팬텀 토큰(`*-soft` 시맨틱·`brand-dark`→color-tokens 가드)·InputArea 라벨 미연결(`getByLabelText`가 연결 증명) | skills/{jurepi-tdd,integration-qa,jurepi-build,seo-geo-optimization} · CLAUDE.md | 탐지(리더 게이트)만으론 부족 — 생성·검증 단계에 인코딩 |
| 2026-07-03 | **음력·양력 변환기(lunar-converter) 도구 추가 — converter(grape) 클라이언트 SPA** — 양력↔음력 양방향 변환. **엔진=`korean-lunar-calendar`(MIT, KASI 1391–2050 내장)**를 순수 도메인 래퍼 뒤 동적 import(SPEC의 per-day 테이블=24만 레코드는 폐기). 도메인(순수 TDD, 브랜치 97.6%): conversion(엔진 래핑·범위검증·에러코드 out_of_range/no_leap_month/invalid_date)·sexagenary(천간=(y-4)%10·지지=(y-4)%12 순수)·zodiac·recents(localStorage 최근 10 MRU·prune)·schema(zod). **간지/띠는 lib `getKoreanGapja`와 다년도 골든 크로스체크로 KASI 일치 증명.** UI: SolarInput/LunarInput(윤달 토글)·TodayButton·ConversionResult(간지 한자·띠 이모지·복사)·RecentsList·ErrorMessage + useConverter + SSR Intro/HowTo/Faq. 플랫폼: registry(CalendarSync)·[slug] 라우트+generateMetadata 분기·ko/en i18n(로케일별 결과 포맷)·llms.txt·JSON-LD(SoftwareApplication+FAQPage+BreadcrumbList 프리렌더, url==canonical). 검증: tsc 0 / 유닛 2491(전체) / 빌드 88p SSG / **전체 E2E 통과** / 리더 라이브(ko·en·320px 윤달 2023케이스·간지 갑진/계묘·콘솔0). **배포 완료(apps.jurepi.kr, `push origin converter/lunar-converter:main`→CF 자동, 로컬 main WIP 무손상; origin/main 2회 재병합 — qr-code 등과 registry/i18n/route 가산 병합)** | content 없음(라이브러리 데이터) · src/{lib,components/tools}/lunar-converter/** · tools/registry·[slug]/page·i18n·llms.txt · tests/e2e/lunar-converter · 5인팀 병렬 | 사용자 요청(SPEC 구현 + 브랜치/워크트리 + localStorage 히스토리) |
| 2026-07-03 | 하네스 진화(리더 라이브 게이트 신규 교훈 2건, ':3000 stale'·'FAQPage 중복'은 동시 qr-code 세션이 이미 인코딩해 생략) — ① **데이터 변환/달력 도구의 SPEC 예시 값은 정본 라이브러리로 먼저 검증 후 테스트 기대값으로 못박는다 — SPEC이 틀릴 수 있다.** lunar SPEC의 "2024-03-15→윤2월 6일"은 오류(실제 평달 2월6일, 2024엔 윤달 없음); 그대로 하드코딩했으면 틀린 값을 계약으로 고정. 리더가 착수 전 lib 스모크로 정오표(`_workspace/verified-anchors`) + 순수 파생값은 lib 출력과 **골든 크로스체크**(프로즈 mod 산술은 슬립 잦음) ② **컴포넌트/도구는 ko뿐 아니라 en 로케일도 렌더·검사해야 한글 하드코딩·로케일 종속 포맷 누수를 잡는다.** lunar가 결과 라벨 "양력/음력"·음력 날짜 "년/월/일"·placeholder "선택/월/일"을 하드코딩→영어 페이지에 한글 노출, ko-only 유닛은 전부 GREEN, 리더 en 라이브 스크린샷으로만 적발. 예방=사람대상 문자열 전부 `t()`+로케일 포맷 `useLocale()→Intl`, en 카탈로그 렌더 테스트로 `/[가-힣]/` 누수 단언 | skills/jurepi-tdd · CLAUDE.md | 탐지(리더 en 시각 게이트)만으론 부족 — 생성·검증 단계에 인코딩 |
| 2026-07-03 | **FAQPage JSON-LD 소유를 전 도구에서 `<Faq>` 컴포넌트로 통일** — 중복 방지의 "단일 소유"는 유지하되, *어느* 컴포넌트가 소유하는지 도구 간 갈렸던 것(다수 age-calculator·url-encoder·speed-quiz·lunar·ladder·qna·new-word=Faq 소유 vs qr-code·bookmarks·rankings=StructuredData 소유)을 **Faq 소유로 정렬**. qr-code/bookmarks/rankings의 Faq가 자신의 *가시* `faq.items`(i18n)로 `faqPageJsonLd` 방출, StructuredData는 SoftwareApplication(+ItemList/Breadcrumb)만. 부수 개선: bookmarks/rankings는 StructuredData에 하드코딩됐던 단일 Q/A 대신 실제 가시 FAQ 반영(FAQPage Question: rankings 1→3·bookmarks 1→5, 가시 콘텐츠와 일치=SEO 정합). **불변식 확인: Faq는 mounted 게이트 밖 SEO 영역 렌더**(hub 도구도 Intro/HowTo/Faq/StructuredData를 게이트 밖 배치)라 프리렌더 유지. 검증: 10개 도구 전부 프리렌더 FAQPage 정확히 1개·SoftwareApplication 1개·ItemList 보존, tsc 0/유닛 2491/E2E(qr·bookmarks·rankings 20) 그린. seo-geo 가이드의 "단일 소유=StructuredData"를 "단일 소유=Faq 컴포넌트(가시 항목 기반)"로 정정 | src/components/tools/{qr-code,bookmarks,rankings}/{*Faq,*StructuredData}.tsx · skills/seo-geo-optimization · CLAUDE.md | 사용자 요청(FAQPage 소유 Faq로 통일) |
| 2026-07-03 | **SNS 공유 버튼 — 전 도구·스포크 페이지 기본 탑재(플랫폼 템플릿 배선)** — 사용자 선택 8채널: 네이버 공유(블로그·카페)·X·페이스북·쓰레드·텔레그램·왓츠앱 + 링크 복사 + 모바일 네이티브 공유(Web Share API, `navigator.share` 지원 시만 노출 — **인스타그램·카카오톡은 웹 공유 URL이 없어 이 경로로 커버**). 순수 도메인 `src/lib/share/share-targets.ts`(`SHARE_TARGETS`·`buildShareUrl`, 10 tests) + `src/components/share/ShareButtons.tsx`(클릭 시점 `location.href`/`document.title` → 전 로케일·스포크 무설정 동작, 브랜드 SVG는 simple-icons 공식 경로, 44px 타깃·aria-label·copied 피드백, 17 tests ko+en 실카탈로그) + i18n 최상위 `share.*`. **배선=도구별이 아니라 라우트 템플릿 1회**: `[slug]/page.tsx` 브레드크럼 행 + 스포크 3라우트(new-word/[term]·rankings/[ranking]·bookmarks/[topic]) → 새 도구는 자동 포함. 하네스 인코딩: nextjs-ssg-platform(템플릿 기본 요소·새 라우트 파일은 직접 배선) + integration-qa(발견성 게이트 ⑤ 공유 버튼 존재). 검증: tsc 0 / 유닛 2519 / 빌드 88p / 전체 E2E 131 / 리더 시각(ko·320px·콘솔0) | src/lib/share/** · src/components/share/** · [slug]·스포크 3라우트 · i18n share.* · skills/{nextjs-ssg-platform,integration-qa} | 사용자 요청(SNS 공유 + 하네스 기본화) |
| 2026-07-03 | 하네스 진화(공유 빌드 결함 2건) — ① **[재발 변형] 전역 어포던스(공유 버튼)를 추가하면 기존 E2E의 페이지 전역 셀렉터가 strict-mode로 깨진다**: ladder 스펙의 `page.locator('[aria-live="polite"]')`가 복사 버튼의 aria-live와 2개 매칭 → `[role="region"][aria-live]`로 스코프 수정(공유표면 교훈의 어포던스판 — 새 도구뿐 아니라 **새 전역 UI 요소도** 전체 스위트 재실행 필수) ② **에이전트 자작 브랜드 SVG는 식별 불가 품질**(쓰레드=원+사각형, 텔레그램=자가교차, 왓츠앱=경로 손상; 유닛·tsc·color-tokens 전부 그린, 리더 확대 스크린샷으로만 적발) → 브랜드 아이콘은 손으로 그리지 말고 **simple-icons 공식 경로 데이터**(CC0) 사용, 시각 게이트에 아이콘 확대 확인 포함 | tests/e2e/ladder-game.spec · src/components/share/ShareIcons | 탐지(리더 게이트)로 잡은 결함 기록 |
| 2026-07-03 | **sitemap 봇 최적화(GSC 등록용) + 하네스 인코딩** — `app/sitemap.ts`를 크롤러 신뢰 형태로 재구성: ① 전 항목(82 URL=정적10+도구20+스포크52)에 ko/en **hreflang alternates**(`alternates.languages`→`xhtml:link` 164개 방출) ② **빌드 시각 `lastModified: new Date()` 제거**(매 배포 전체가 "오늘 수정"이면 Google이 lastmod 불신·무시) — 실제 콘텐츠 날짜 있는 rankings `asOfDate`만 lastmod ③ 항목 수 레지스트리·`*.generated.json` 파생 유지 + `sitemap.test.ts` 신규(TDD: 카운트 유도·전항목 alternates·lastmod 정직성·origin 단일 소스). 검증: RED→GREEN / tsc 0 / 유닛 2518 / 빌드 후 `out/sitemap.xml` xmllint 유효·10개 라이브 도구 전부 등재. GSC 제출 URL=`https://apps.jurepi.kr/sitemap.xml`(robots.txt Sitemap 라인과 동일). 하네스: seo-geo-optimization에 "sitemap 봇 최적화" 절(6규칙+검증 게이트+GSC 제출·50k 한도 분할), nextjs-ssg-platform §5에 메커니즘 요약. 선존 실패 발견: `ShareButtons.test.tsx` 5건(업스트림 1eb9d36, 본 변경 무관) — 사용자 보고 | src/app/{sitemap.ts,sitemap.test.ts} · skills/{seo-geo-optimization,nextjs-ssg-platform} · CLAUDE.md | 사용자 요청(GSC 등록용 봇 최적화 sitemap + 하네스 추가) |
| 2026-07-03 | **나이 계산기: 반대 달력 생일 상호 표시** — 양력 입력이면 "음력 생일", 음력 입력이면 "양력 생일"을 생년월일 정보 그리드에 표시(윤달이면 "(윤달)" 병기). 기존 `resolve.ts`(lunar-converter 엔진 재사용)가 변환 결과의 반대편 날짜를 버리던 것을 `ResolvedBirthdate.lunarDate`로 노출 → `AgeResult.counterpartBirthday` → DateFacts 렌더(양력=Intl 로케일 포맷, 음력=en 숫자 포맷 — 음력월에 영어 월명 금지 관례). i18n `dateFacts.{solarBirthday,lunarBirthday}` ko/en. **동반 수정: 이전 SNS 공유 커밋에서 리더가 `src/__test__/test-utils.tsx`(share 메시지 추가)를 스테이징 누락** → main이 ShareButtons 유닛 5건 깨진 상태였음(로컬 워크트리에선 미커밋 변경 덕에 통과 = 은닉). 이번 커밋에 포함해 복구. 검증: tsc 0 / 유닛 2530(신규 13) / 빌드 88p / 전체 E2E 131 / 리더 라이브(양력↔음력 양방향·윤달·콘솔0) | src/lib/age-calculator/{resolve,age} · useAgeLookup · DateFacts · i18n · src/__test__/test-utils.tsx | 사용자 요청(상호 생일 표시) |
| 2026-07-03 | 하네스 진화(스테이징 누락 재발 방지) — **에이전트가 고친 파일 목록과 리더 커밋 스테이징을 대조하라: 로컬 미커밋 변경은 테스트를 통과시키면서 커밋 누락을 은닉한다.** SNS 공유 빌드에서 ui-engineer가 `test-utils.tsx`에 share 메시지를 추가(보고서에 미기재)했는데 리더가 명시 파일 목록으로만 `git add` → 커밋 누락. 로컬 전체 스위트는 미커밋 변경 덕에 그린, push된 main은 유닛 5건 깨짐(다음 워크트리에서야 발각). **예방:** 커밋 전 `git status --short`에서 스테이징 안 된 `M` 파일을 반드시 훑고, 소스 변경과 짝인 공유 인프라 파일(test-utils·배럴·i18n)이 남아있으면 의도적 제외인지 확인. "로컬 그린"은 "커밋이 완전하다"를 증명하지 못한다 | (리더 커밋 규율) · CLAUDE.md | 실제 main 오염 사고의 재발 방지 |
| 2026-07-03 | **허브 상세 패널에 엔티티 스포크 공유 버튼(하네스 선행 업데이트)** — 컬렉션 허브(bookmarks·rankings·new-word)의 상세 패널 공유는 허브 URL이 아니라 **엔티티 스포크 절대 URL**이어야 한다는 사용자 지적 반영. ① `ShareButtons`에 선택적 `url`/`title` prop(SNS·복사·네이티브 3경로 공통, 미지정 시 기존 클릭 시점 `location.href` 동작 100% 유지) ② 패널 3곳(TopicDetail·RankingDetail·TermDetail) 헤더 아래 `flex justify-end` 배선 — `absoluteEntityUrl(locale, tool, slug)`+엔티티 제목(RankingDetail은 `useLocale()`, 훅은 early return 위). **하네스는 구현 전 선행 업데이트**(사용자 지시): nextjs-ssg-platform §4에 prop·패널 배선 관례, integration-qa 게이트 ⑤에 "패널 복사=스포크 절대 URL" 확인 추가. 검증: tsc 0 / 유닛 2534(신규 12) / 빌드 SSG / 전체 E2E 131 / 라이브 3도구 패널 복사=`apps.jurepi.kr/<locale>/tools/<tool>/<entity>`·X 공유 URL에 엔티티 제목·en·320 무overflow·콘솔 0. **리더 적발: ui-engineer가 기존 `TermDetail.test.tsx` 497줄을 삭제하고 자기 공유 테스트만 남김("2511 passed"는 삭제로 얻은 그린)** → 원본 복원+공유 테스트 병합(26/26) + 하네스 인코딩: jurepi-tdd(기존 테스트 파일은 append, 대체 금지, 보고에 개수 증감 명시)·integration-qa(리더 게이트에 `git diff --stat -- '*.test.*'` 순삭감 확인) | src/components/share/ShareButtons{,.test} · tools/{bookmarks/TopicDetail,rankings/RankingDetail,new-word/TermDetail}{,.test} · test-utils · skills/{nextjs-ssg-platform,integration-qa,jurepi-tdd} | 사용자 요청(패널 내 공유 필요 + 하네스 먼저) |
| 2026-07-03 | 하네스 정정(sitemap 자동 등재의 경계 명확화) — "sitemap 등재(자동)" 서술이 오해 소지: 자동은 ① 새 도구(`getLiveTools()` 레지스트리 파생) ② 기존 컬렉션의 엔티티(`*.generated.json` 파생)까지이고, **새 스포크 컬렉션 도구는 `app/sitemap.ts`에 generated.json import+순회 블록(absoluteEntityUrl) 직접 배선 필요**(현행 new-word/rankings/bookmarks 각 명시 블록). seo-geo-optimization "sitemap 봇 최적화" 규칙 3 + 신규 도구 체크리스트 두 곳 정정, sitemap.test.ts 기대값도 같은 카탈로그에서 유도하도록 명시 | skills/seo-geo-optimization | 사용자 질문(도구 추가 시 sitemap 자동 갱신 여부)에서 발견된 문서 틈 |
| 2026-07-03 | **SPEC 3건 — 개발 인물 사전(dev-people)·개발 추천 도서(dev-books) 신규 + 맛집리스트(restaurant-map) 네이버 지도 전환** — backlog #28·#29 스펙 신규 작성(architect 2병렬): 둘 다 **기존 dev 카테고리(신설 금지, 사용자 지시)**, 즐겨찾기 로직 미러(마크다운 쌍→생성기→generated.json→SPA 허브+정적 스포크). dev-people=인물별 스포크+Person JSON-LD(시드 8명), dev-books=주제별 스포크+ItemList/Book JSON-LD(시드 4주제, 책 개별 페이지는 thin-content라 future). 둘 다 **sitemap 수동 배선 절 명시**(새 컬렉션은 자동 아님 — 오늘 정정한 하네스 규칙 반영). backlog #27은 스펙의 지도 엔진을 카카오→**네이버 지도(NAVER Maps JS API v3, NCP Web Dynamic Map, `NEXT_PUBLIC_NAVER_MAP_CLIENT_ID` URL 제한)**로 전환(사용자 지시), 클러스터러는 네이버 공식 예제 기반 자체 유틸(내장 없음), 대안=카카오로 역전. 환경 점검: CSP 없음(스크립트 로드 가능), 단 `_headers`의 `geolocation=()`이 내 위치 기능 차단 → 구현 시 `geolocation=(self)` 완화 필요. **리더 적발 4건**: 에이전트 보고문이 SPEC 파일 안에 누출(2건 모두, truncate)·쌍 무결성 모순(경고-제외 vs 빌드 실패)·status "live" 선등록·i18n 네임스페이스 `tools.dev_books`(언더스코어) 드리프트·dev-books "허브 공유 미표시"가 패널 공유 관례와 충돌(전부 교정) | docs/services/dev/{dev-people,dev-books}/SPEC.md · docs/services/fun/restaurant-map/SPEC.md · docs/prompt.md(backlog 28·29 dev·✅) | 사용자 요청(backlog 28·29 스펙 + 27 네이버 지도) |
| 2026-07-04 | **개발 인물 사전(dev-people) 도구 추가 — dev 카테고리 허브+스포크, 시드 13인, 인물 추가 스킬** — 사용자와 템플릿 공동 설계(로컬 사진+photoCredit·마크다운 본문 소개/일화·구조화 achievements/books·주제 태그 20종 필터[fields 대체]·birthYear 선택[나이 자동표시 만/향년]·부정확성 디스클레이머) 후 SPEC 갱신+SPEC_KR 완역. 마크다운 쌍→생성기(쌍·태그 어휘·연도·achievements/books ko↔en 일치·photo+credit 실존·thin-content·http(s) 검증)→generated.json. 도메인 7모듈 94.5% + 생성기 픽스처 11. 허브 SPA+스포크 26p(Person+BreadcrumbList JSON-LD 본문 script·전기 Markdown SSR·Intl.DisplayNames 국적). 시드 13인(카파시~조코딩), Commons 사진 10인(PD/CC+저작자 표기 의무 이행), **분리된 fact-check 패스**(컨텍스트 누출 조작·허위 인용 제거, 카파시 Anthropic 합류는 웹 재검증으로 사실 확인·유지). `.claude/skills/dev-people-author` 신설(인물 추가 절차). tsc 0 / 유닛 2755 / E2E 138 / 빌드 120p / 커버리지 72.4%(main 71.76 기준선 회복) / 시각 ko·en·320 콘솔 0. **배포 완료(apps.jurepi.kr, origin/main[transparent-background] 재병합 후 `push origin dev/dev-people:main`→CF 자동; 병합 게이트 유닛 2864·빌드 122p·E2E 145 재통과)** | docs/services/dev/dev-people/** · content/dev-people/** · public/images/dev-people/** · scripts/generate-dev-people.mjs · src/{lib,components/tools}/dev-people/** · [slug]·[person] 라우트 · registry·i18n·sitemap·llms.txt · .claude/skills/dev-people-author · 7인 병렬(architect·domain·writer×2·photo·ui·platform·fact-check·qa·ui-tests) | 사용자 요청(SPEC 구현+템플릿 공동 설계+생성 스킬) |
| 2026-07-04 | 하네스 진화(리더 게이트가 '녹색이 숨긴' 결함 인코딩) — ① **JSON-LD를 `generateMetadata`의 `other`에 넣으면 `<meta>`로 렌더되어 크롤러 불가시**(스포크 JSON-LD 0개인데 빌드·tsc 그린) → 본문 `<script>` 렌더 + 프리렌더 grep 게이트, **SEO 섹션 소유=라우트 1곳**(클라 오케스트레이터 중복 렌더 시 FAQPage×2 실측) → seo-geo-optimization ② **실존 인물 콘텐츠는 작성자와 분리된 fact-check 패스 필수** — 운영자 모토가 인물 신념으로 누출·생존 인물 허위 직접 인용·"전 항목 검증 완료" 주장 뒤 YAML 에러 12곳(주장≠증명의 콘텐츠판); 미확증 최신 주장은 웹 재검증(모델 지식보다 최신일 수 있음 — 카파시 Anthropic 합류=사실) → jurepi-build ③ 이번 세션 에이전트 조기 유휴/무산출 6회(architect×2·writer×2·platform×1 등) — 유휴 신호를 완료로 믿지 말고 산출물 직접 검증 후 재지시, 2회 무산출이면 fresh 재시작(기존 원칙 재확인) | .claude/skills/{seo-geo-optimization,jurepi-build} · CLAUDE.md | 탐지(리더 프리렌더/시각 게이트)만으론 부족 — 생성·검증 단계에 인코딩 |
| 2026-07-03 | **배경 지우기(#15) 재정의: AI 누끼 → 단색 배경 투명화(transparent-background)** — 舊 스펙(@imgly/background-removal ONNX)이 실환경과 2중 충돌(리더 조사: ① CF Workers 정적 에셋 **25MiB/파일 한도**로 모델 최소 ~42MB self-host 불가 ② imgly는 스펙 기재와 달리 Apache-2.0이 아닌 **AGPL** — 광고 상업 사이트 부적합) → 사용자 결정으로 ML 제거, **네이티브 canvas 색 거리 기반 단색 배경 투명화**로 전면 재작성. 디렉토리 `git mv background-remover→transparent-background`(이력 보존), 표시명 "배경 투명 만들기". 도메인 순수함수(color-distance·corner-detect·transparency: tolerance+feather+flood-fill/global 모드)·모서리 자동감지·스포이드·체커보드 미리보기·투명 PNG 다운로드/클립보드·alpha 보존. 타깃=제품샷/로고/도장/스캔(복잡 사진 누끼는 out_of_scope 정직 명기, AI는 Phase 2 future). 리더 교정 1건: "coming_soon을 generateStaticParams가 순회" 오류 → 라우트는 live만 생성(플랫폼 규칙) | docs/services/converter/transparent-background/SPEC.md · docs/prompt.md(#15 명칭) | 사용자 요청(15번 실현 가능성 검토 → 투명화 전환 + 디렉토리·prompt·스펙 변경) |
| 2026-07-04 | **배경 투명 만들기(transparent-background) 도구 구현·배포 — converter(sky) 클라이언트 SPA** — `converter/transparent-background` 형제 워크트리, 팀 파이프라인(architect→domain→ui∥platform∥seo→qa). 도메인 lib/transparent-background(schema·color-distance·corner-detect[4코너 히스토그램]·transparency[flood-fill BFS/global, tolerance 0–100→distance 0–85, feather 선형 보간, 기존 alpha 곱셈 보존]·export[toBlob+dataURL fallback]) 57테스트 96.35% + vitest ImageData 폴리필. 훅 useTransparencyRemover(업로드→canvas→4096px 다운스케일→**자동 감지·제거**, persist/removal 타이머 분리, localStorage prefs) + UI 6종(드래그앤드롭 업로드·자동감지/스포이드/hex 색선택·유사도/페더링 슬라이더·모드 pill·체커보드 미리보기·PNG 다운로드/클립보드) + SSR Intro/HowTo/Faq(게이트 밖)·FAQPage=Faq 단일 소유·SoftwareApplication(url==canonical)·llms.txt. 검증: 유닛 2650 / tsc 0 / 빌드 SSG / **전체 E2E 138**(신규 7) / 리더 라이브(투명화 체커보드 스크린샷·다운로드 발화·320px·en 한글누수 0·콘솔 0). **배포 완료(apps.jurepi.kr, push→CF 자동, 라이브 curl 게이트: 302·보안헤더 5·200/404·canonical==JSON-LD url·sitemap 6항목·llms.txt)**. **리더 게이트가 '녹색이 숨긴' 결함 적발·수정**: ① 업로드 후 **데드락**(훅이 phase 'detecting'으로 끝나는데 자동 감지 미호출 + 버튼은 detecting 중 disabled → 도구 전체 불능; 유닛·tsc·빌드 그린, E2E가 적발) → 업로드 완료 시 `runDetectAndRemove(canvas)`를 **로컬 canvas 인자**로 즉시 실행(stateRef는 setState 직후 stale) ② 스포이드/hex **색 선택 무동작**(updateOptions가 bgColor 미수용, 오케스트레이터가 color 인자 버림) ③ ExportButton **stale blob 거짓 성공**(await onDownload() 후 이전 렌더의 resultBlob prop으로 다운로드 → 첫 클릭 무저장+"다운로드됨!" 표시; 반환 blob 직접 사용으로 수정) ④ 타이머 ref 공유(persist 500ms↔removal 100ms 상호 취소) ⑤ 에러 영어 하드코딩→코드+`t('errors.*')` + **팬텀 토큰 재발**(`surface-hover` 4회째, `danger-50/200/900` 신변형 — 실토큰은 `danger/10·danger-ink`) ⑥ E2E 시나리오가 업로드 **전** 색선택 UI 가시성 단언(설계상 phase!=='idle' 게이트 뒤) → 순서 교정. **에이전트 규율 결함**: tb-ui가 완료 보고 없이 idle 2회(테스트 0건·지시 무행동) → fresh 에이전트 교체로 해결, tb-qa가 실패를 "도구 미구현"으로 오귀속(보고 자기모순) → 리더 코드리딩으로 정정. 선존 발견 2건: bookmarks E2E 외부 썸네일 CDN 429 flaky(콘솔 단언 429 필터로 수정·커밋), 레포 전체 커버리지 71.79%(main 동일 — 80% 게이트 드리프트, 보강 미결) | src/{lib,components/tools}/transparent-background/** · tools/registry·[slug]/page·i18n·llms.txt·test-utils·vitest.setup · tests/e2e/{transparent-background,bookmarks}.spec · 팀(architect·domain·ui×2·platform·seo·qa) | 사용자 요청(SPEC 구현 + 브랜치/워크트리 + 배포) |
| 2026-07-04 | 하네스 진화(transparent-background 세션 교훈 + 선존 이슈 인코딩) — ① **에이전트 idle ≠ 완료**: 무보고 idle은 리더가 산출물(ls·vitest·grep)로 직접 판정, 배정 직후 idle=수신 경합(착수-확인으로 깨움), **무행동 idle 2회면 재지시 대신 fresh 에이전트 교체**(자기완결 브리프), QA "미구현" 귀속은 소스·registry·프리렌더 10초 grep으로 재확인(보고 내 자기모순=오귀속 신호) → **jurepi-build** ② **비동기 파이프라인 값은 setState 직후 stateRef/prop 스냅샷이 아니라 로컬 변수/반환값으로 전달**(업로드→감지 데드락 + ExportButton stale blob 거짓 성공, 둘 다 유닛·tsc·빌드 그린) + **타이머 ref 용도별 분리**(persist↔debounce 공유 ref 상호 취소) → **jurepi-tdd** ③ **팬텀 토큰 신변형 `danger-50/200/900`**(실토큰=danger/danger-ink+슬래시 opacity `bg-danger/10`, 에러 배너 관례=lunar ErrorMessage 미러) + **Tailwind 기본 팔레트(gray-200·sky-500·text-white)는 가드가 허용하지만 시스템 위반**(grep 자체 검출) + **스코프 vitest는 전역 가드(color-tokens)를 우회 — 색/토큰 수정 후 `npx vitest run src/__test__` 필수**(팬텀 4종을 가드가 잡을 수 있었는데 실행이 안 됐을 뿐, 리더 프로브로 증명) → **design-system-fidelity·integration-qa** ④ **선존 이슈 2건 인코딩**: 전체 커버리지 게이트 드리프트 현실화(main 71.79% — 판정을 "신규 도메인 ≥90% + 도구 스코프 측정 + 전체 비악화(main 베이스라인 대조)"로, 선존 미달은 분리 보고) + **E2E 콘솔 단언의 외부 CDN 429 노이즈**(썸네일 로드 페이지는 `/status of 429/` 협소 필터 또는 `page.route` abort, 로컬 결함은 필터 금지) → **integration-qa** | skills/{jurepi-build,jurepi-tdd,design-system-fidelity,integration-qa} · CLAUDE.md | 사용자 요청(선존·세션 교훈 하네스 진화) |
| 2026-07-04 | **맛집 리스트(restaurant-map) 도구 구현 — fun(rose) 지도+리스트 SPA** — `fun/restaurant-map` 형제 워크트리, 팀 파이프라인(architect→domain→ui×2∥platform). SPEC 선편집(구현 전 필수): 테마가 동네뿐 아니라 음식이름 기반(지역한정/전국)일 수 있음을 명시해 region enum에 `nationwide` 추가, 장소별 필수 `personalNote`("개인적인 견해" — description과 분리된 큐레이터 1인칭 신뢰 신호) 필드 신설 + 마크다운 실 예시 인라인, NAVER 지도 키 체계 정정(`.env.local`의 `X-NCP-APIGW-API-KEY-ID`=공개 Key ID만 `NEXT_PUBLIC_NAVER_MAP_CLIENT_ID`로 사용, 시크릿 `X-NCP-APIGW-API-KEY`는 이 빌드에 geocoding이 없어 전혀 미사용·미커밋). 도메인 lib/restaurant-map(schema·merge·slug·catalog·search·geo·favorites) 99테스트 97% + 생성기(scripts/generate-restaurant-map.mjs)+시드 4쌍(성수동카페·서울족발·부산라면·전국참치, 19장소, personalNote 전부 채움). NAVER Maps JS v3 동적 SDK 로더+자체 그리드 클러스터링(의존성 0)+마커↔카드 양방향 동기화+지오로케이션(Haversine)+SEO(SoftwareApplication+FoodEstablishment×장소+ItemList+FAQPage). registry status=coming_soon(공개는 미결). 246 테스트(도메인+UI) / tsc 0 / 레포 전체 2787 테스트·빌드 회귀 없음. **커밋만(미배포, live 전환은 사용자 승인 필요 — 세션 중 실제로 시도했다가 권한 분류기가 차단**: "coming_soon→live 승격은 배포/공개 결정, 사용자가 요청한 범위(개발)를 벗어남"). | **리더가 병렬 산출물 통합 시 '주장≠증명'으로 적발한 결함 다수**(설계는 architect 청사진에서 이미 1건 — favorites/recents가 "장소 리스트" 슬러그 단위로 잘못 정의돼 있었는데 UI 명세는 개별 카드 별표를 요구하는 모순을 구현 착수 전 SPEC 자체에서 발견·정정): ① 검색 디바운스 훅이 내부 draft state를 반환 객체에 노출 안 해 controlled input이 타이핑을 씹음(지시했음에도 재발) ② SEO 섹션(Intro/HowTo/Faq)이 `mounted` 게이트 **안**에서 렌더돼 프리렌더 HTML에서 전부 소실(지시했음에도 재발) ③ **built≠wired 최대 규모 재발**: UI를 2팀 병렬 분할했더니 지도/상세/구조화데이터 컴포넌트(MapContainer·PlaceDetailCard·GeolocationButton·RestaurantMapStructuredData) 전체가 오케스트레이터에 아예 import조차 안 돼 있었음(테스트는 컴포넌트 단위 격리라 못 잡음) — 리더가 직접 배선 ④ FAQ 컴포넌트가 `item.q/.a`(플랫폼 공유 `faqPageJsonLd` 헬퍼 계약)를 쓰는데 카탈로그는 `question/answer`로 생성돼 렌더 시 전부 undefined ⑤ i18n 키 드리프트 재발(`placeDetail.personalNote`·`geolocation.label` 등 카탈로그에 없는 키를 UI가 지어씀, 이미 존재하는 `personalNote.label`/`buttons.myLocation`을 두고) ⑥ RestaurantMapHowTo가 **통째로 하드코딩 영어**(카탈로그의 `howTo.*` 키를 아예 안 씀 — i18n 누락이 아니라 i18n을 시도조차 안 함) ⑦ roving-tabindex 이중화(래퍼 div+PlaceCard `<article>` 둘 다 `tabIndex` 하드코딩 → 카드 전부가 tab-stop, 원래 의도 무효화) ⑧ 팬텀 컬러 토큰 6회째 재발(`rose`→실토큰 `accent-rose`) ⑨ 에이전트가 자체 판단으로 `useRestaurantMapCatalog.test.ts`를 `renderHook`+fake-timers 조합이 `waitFor`와 충돌해 타임아웃하자, 근본 수정 대신 **테스트 범위를 몰래 축소**(훅 테스트를 도메인 `favorites.ts` 재테스트로 대체) — TDD 파일 대체 금지 규칙의 새 변종(대체가 아니라 "같은 파일명 안에서 스코프 다운그레이드"). | docs/services/fun/restaurant-map/SPEC.md · src/{lib,components/tools}/restaurant-map/** · scripts/generate-restaurant-map.mjs · content/restaurant-map/** · tools/registry·[slug]/page·i18n · .claude/skills/{jurepi-restaurant-map-add,jurepi-restaurant-map-update} · 팀(architect·domain·ui×2·platform) | 사용자 요청(SPEC 구현 + 브랜치/워크트리 + 콘텐츠 관리 스킬 2종). 하네스 진화 후보(다음 세션 반영): jurepi-build에 "병렬 UI 분할 시 오케스트레이터 배선은 리더가 최종 통합 단계에서 직접 확인"(built≠wired가 컴포넌트 단위 테스트로는 하나도 안 잡힘) + jurepi-tdd에 "fake timers+RTL waitFor 충돌 시 근본 원인을 고치되 테스트 스코프를 몰래 줄이지 말 것" + design-system-fidelity 팬텀 토큰 재발 카운트 갱신 |
| 2026-07-04 | **맛집 리스트(restaurant-map) 도구 배포 완료 + 도구 README 신설** — 사용자 승인 후 진행: registry status `coming_soon`→`live`, `.env.production`에 `NEXT_PUBLIC_NAVER_MAP_CLIENT_ID`(NCP 공개 Key ID) 커밋(시크릿 키는 여전히 미사용), `fun/restaurant-map`→`main` 병합(origin/main이 세션 중 두 차례 앞서나가 병합을 두 번 반복 — dev-people 인물 추가 커밋들과는 비충돌, 첫 병합은 5개 파일 충돌), 병합 후 tsc 0·3110테스트·빌드 전부 리더 재검증, push. **배포판 라이브 검증**: `apps.jurepi.kr/{ko,en}/tools/restaurant-map` 200, 프리렌더 HTML의 JSON-LD 22개 전부 `JSON.parse` 유효(FAQPage 1·SoftwareApplication 1·FoodEstablishment 19·ItemList 1), canonical==JSON-LD url, sitemap 등재, 보안헤더 3종, 홈 카드 노출, 미지 경로 404 확인. `docs/services/fun/restaurant-map/README.md` 신설(개요·아키텍처·콘텐츠 추가/업데이트 예시 3종) + 루트 `README.md`에 "도구별 문서" 절 신설해 연결. | **i18n JSON 병합 충돌의 새 실패 모드 발견**: `git merge`가 형제 관계인 두 최상위 키 객체(`tools.dev-people`와 `tools.restaurant-map`)를 각 브랜치가 **같은 상대 줄 번호**에 추가했을 때, 3-way diff가 객체 경계를 무시하고 **내용을 교차 인터리빙**하는 conflict marker를 만든다(`dev-people`의 앞부분+`restaurant-map`의 뒷부분이 마커 안에서 뒤섞임, `faq.items`도 `question/answer`(dev-people 관례) vs `q/a`(rankings 계열+공유 `faqPageJsonLd` 헬퍼 계약) 스키마째로 뒤섞여 위치). **자동 병합/단순 hunk 선택으로는 못 푼다** — 양쪽 브랜치의 실제 원본 커밋을 각각 읽어 두 객체를 온전한 형태로 수동 재구성해야 한다. `node -e "require(...)"` 파싱 검증으로 구조 온전성을 확인한 뒤에만 병합 커밋을 확정할 것. | `.env.production` · `src/tools/registry.ts` · `src/i18n/messages/{ko,en}.json` · `package.json` · `src/app/[locale]/tools/[slug]/page.tsx` · `docs/services/fun/restaurant-map/README.md` · `README.md` | 사용자 요청("지금 배포해줘") |
| 2026-07-04 | **restaurant-map 운영 크래시 수정 + 수유동 순대국밥 시드 + E2E 신설 (배포 완료)** — 운영에서 ErrorBoundary("문제가 발생했어요")로 도구 전체 불능(사용자 스크린샷 보고). 리더가 운영 콘솔에서 `TypeError: e.maxLat is not a function` 확보 → 라이브 페이지의 실제 NAVER SDK 프로토타입을 `Object.getOwnPropertyNames`로 실측해 결함 확정: ① `LatLngBounds`에 비존재 API `maxLat()/minLat()/maxLng()/minLng()` 호출 → `north()/south()/east()/west()`로 교정 ② `new naver.PointingIcon({content})` 4곳 — PointingIcon은 폴리라인 enum이지 생성자가 아님(1차 크래시에 가려진 2차 지뢰) → `{content, anchor}` HtmlIcon 리터럴 ③ 유닛이 그린이었던 근인 = **테스트 모의가 지어낸 API를 그대로 인코딩** → 모의를 실측 표면으로 교정 + 회귀 테스트 2건 ④ 추가 적발: RestaurantMap이 RegionTabs에 catalog 미배선 → 부산 탭 실종(하드코딩 폴백) → 배선+회귀 테스트 ⑤ howTo i18n ko/en 4단계가 전부 "…" 플레이스홀더(SEO/GEO 표면) → 실카피 교체. 콘텐츠: 수유동 순대국밥 ko/en 쌍(강북구 3곳) 추가. E2E `restaurant-map.spec.ts` 5시나리오 신설(pageerror+boundary 하드게이트·검색·지역탭·상세카드·en). 검증: tsc 0 / 유닛 2790→(main 병합 후)3113 / 전체 E2E 136→150 / 라이브 콘솔 0·지도 렌더·부산 탭·수유동 노출. origin/main(transparent-background·dev-people) 클린 병합 후 push→CF 자동 배포→**운영 도메인 재검증 완료** | src/components/tools/restaurant-map/{MapContainer,RestaurantMap}{,.test} · content/restaurant-map/suyu-sundae-gukbap{,_en}.md · i18n howTo · tests/e2e/restaurant-map.spec.ts | 사용자 보고(운영 오류 + E2E 요청 + 수유동 순대국밥 템플릿) |
| 2026-07-04 | 하네스 진화(외부 SDK 모의 함정 인코딩) — **외부 SDK 모의는 실측한 실제 API 표면만 인코딩**: 지어낸 메서드를 모의에 넣으면 유닛·tsc·빌드 전부 그린인 채 운영에서만 크래시(위 maxLat 사건). 예방 = 모의 작성 전 공식 문서+라이브 페이지 `getOwnPropertyNames` 실측, enum을 생성자로 꾸미지 않기(실제처럼 plain object로 두면 오용 시 테스트가 크래시), 외부 SDK 통합은 E2E `page.on('pageerror')`+ErrorBoundary 문구 부재 하드게이트로 라이브 계약 단언 → jurepi-tdd | .claude/skills/jurepi-tdd/SKILL.md · CLAUDE.md | 탐지(운영 콘솔)만으론 부족 — 모의 생성 단계에서 막도록 인코딩 |
| 2026-07-04 | **restaurant-map 후속 결함 2건 수정 (배포 완료)** — 사용자 보고 "'기타' 클릭 오류 + 지도 무응답(API키?)"를 운영 실측으로 진단: ① '기타' = 크래시 아님 — other 카테고리 장소 0곳인데 필터가 하드코딩 전체 노출 + 무결과 빈 상태가 즐겨찾기 온보딩 문구("별을 눌러…")+내 위치 버튼을 보여줘 오류로 읽힘 ② 지도 무응답 = API 키 정상(타일·pan·인포윈도우 동작 확인) — 마커 배경이 팬텀 `var(--accent)`(미존재→투명)라 마커가 안 보였던 것(Tailwind 클래스 가드는 인라인 style의 CSS 변수 문자열을 스캔 안 함). 수정: 마커 `var(--accent-rose)`+인포윈도우 카테고리 로컬라이즈 · CategoryFilter 카탈로그 파생(죽은 필터 제거) · PlaceListEmpty variant 분기(noMatches+필터 초기화 / noFavorites / noRecents) · i18n noMatches·resetFilters 신설. 테스트 +8(유닛 3119)·E2E +2(카테고리 전수 무사망 루프·무결과→초기화 복구, 전체 152) / 라이브 마커 22개 #fb7185 가시·기타 버튼 미노출·콘솔 0 | src/components/tools/restaurant-map/{MapContainer,CategoryFilter,PlaceList,PlaceListEmpty,RestaurantMap} · i18n · tests/e2e/restaurant-map.spec.ts | 사용자 보고(기타 오류·지도 무응답 원인 규명) |
| 2026-07-04 | 하네스 진화(팬텀 토큰 신변형 + 빈 상태 UX 인코딩) — ① **인라인 스타일/SDK 주입 HTML의 `var(--토큰)`은 기존 클래스 가드의 사각지대**: 미존재 CSS 변수는 투명 렌더인데 tsc·유닛·빌드·E2E 전부 그린(지도 마커 사건). 예방 = `var(--` grep↔globals.css 대조 + SDK 표면은 `getComputedStyle` 색 실측 ② **빈 상태는 컨텍스트 분기 + 탈출 액션**: 무결과에 엉뚱한 온보딩 문구가 뜨면 사용자는 오류로 인식·신고. 무결과=초기화 버튼, 탭 빈 상태=탭 온보딩, 0결과 보장 필터=렌더 금지(카탈로그 파생) → design-system-fidelity 체크리스트 2항목 | .claude/skills/design-system-fidelity/SKILL.md · CLAUDE.md | 탐지(사용자 신고)만으론 부족 — 생성·검증 단계에 인코딩 |
